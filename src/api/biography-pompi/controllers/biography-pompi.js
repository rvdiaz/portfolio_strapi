'use strict';

/**
 * biography-pompi controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::biography-pompi.biography-pompi');
